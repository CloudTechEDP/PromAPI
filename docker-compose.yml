services:
  promapi:
    build:
      context: .
    ports:
      - "8001:8001"
    volumes:
      - ./database:/app/database
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn main:app --host 0.0.0.0 --port 8001


    
